package com.stockflow.payment.internal.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller for the payment module. STARTER STUB — no endpoints yet. See
 * {@code customer.internal.controller.CustomerController} for the endpoint checklist.
 */
@RestController
@RequestMapping("/api/v1/payments")
class PaymentController {

    // TODO: inject PaymentService via the constructor and add guarded endpoints.
}
