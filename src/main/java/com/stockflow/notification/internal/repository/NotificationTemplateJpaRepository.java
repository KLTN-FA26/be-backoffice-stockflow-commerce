package com.stockflow.notification.internal.repository;

import com.stockflow.notification.internal.entity.NotificationTemplateJpaEntity;
import com.stockflow.common.persistence.BaseJpaRepository;

/** Spring Data repository for {@link NotificationTemplateJpaEntity}. STARTER STUB. */
interface NotificationTemplateJpaRepository extends BaseJpaRepository<NotificationTemplateJpaEntity> {

    // TODO: add finders, e.g. findByCodeAndLocale.
}
