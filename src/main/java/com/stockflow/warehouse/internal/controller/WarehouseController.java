package com.stockflow.warehouse.internal.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller for the warehouse module. STARTER STUB — no endpoints yet. See
 * {@code customer.internal.controller.CustomerController} for the endpoint checklist.
 */
@RestController
@RequestMapping("/api/v1/warehouses")
class WarehouseController {

    // TODO: inject WarehouseService via the constructor and add guarded endpoints.
}
