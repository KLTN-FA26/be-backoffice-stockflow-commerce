package com.stockflow.reporting.internal.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller for the reporting module. STARTER STUB — no endpoints yet. See
 * {@code customer.internal.controller.CustomerController} for the endpoint checklist.
 */
@RestController
@RequestMapping("/api/v1/reports")
class ReportingController {

    // TODO: inject ReportingService via the constructor and add guarded read endpoints.
}
