package com.stockflow.chat.internal.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller for the chat module. STARTER STUB — no endpoints yet. See
 * {@code customer.internal.controller.CustomerController} for the endpoint checklist.
 */
@RestController
@RequestMapping("/api/v1/conversations")
class ChatController {

    // TODO: inject ChatService via the constructor and add guarded endpoints.
}
