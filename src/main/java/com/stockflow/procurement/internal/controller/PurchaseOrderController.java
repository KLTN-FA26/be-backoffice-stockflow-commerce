package com.stockflow.procurement.internal.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller for the procurement module. STARTER STUB — no endpoints yet. See
 * {@code customer.internal.controller.CustomerController} for the endpoint checklist.
 */
@RestController
@RequestMapping("/api/v1/purchase-orders")
class PurchaseOrderController {

    // TODO: inject ProcurementService via the constructor and add guarded endpoints.
}
