package com.stockflow.fulfillment.internal.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller for the fulfillment module. STARTER STUB — no endpoints yet. See
 * {@code customer.internal.controller.CustomerController} for the endpoint checklist.
 */
@RestController
@RequestMapping("/api/v1/fulfillment")
class FulfillmentController {

    // TODO: inject FulfillmentService via the constructor and add guarded endpoints.
}
